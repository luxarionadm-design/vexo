/**
 * FormHandler - Entry point
 * Export only what's needed
 */
export { FormHandler, createFormHandler } from './FormHandler.js';
export { FORM_CONFIG } from './config.js';
export { isValidEmail, isValidPassword } from './utils/validators.js';
